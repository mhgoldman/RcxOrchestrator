<p id="no_batch_commands_msg"<%= ' style="display: none;"'.html_safe if batch_commands.any? %>>There are no commands.</p>
<table class="table" id="commands_table" <%= ' style="display: none;"'.html_safe unless batch_commands.any? %>>
	<thead>
		<tr>
			<th>Index</th>
			<th>Command</th>
			<% unless defined?(statuses) && !statuses %>
				<th>Status</th>
			<% end %>
		</tr>
	</thead>
	<tbody>
		<% batch_commands.each do |batch_command| %>
			<tr>
				<td><%= batch_command.index %></td>
				<td><%= link_to batch_command.command.name, batch_command.command %></td>
				<% unless defined?(statuses) && !statuses %>
					<td><%= link_to step_status_for(batch_command), client_batch_commands_path(batch_command_id: batch_command) %></td>
				<% end %>
			</tr>
		<% end %>
	</tbody>
</table>